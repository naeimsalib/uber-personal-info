worker_processes 1;
events { worker_connections 1024; }

http {
  server {
    listen <%= ENV["PORT"] %>;
    server_name _;
    
    location / {
      root <%= ENV['HOME'] %>;
      index index.html;
      try_files $uri /index.html;
    }
  }
}
